document.addEventListener("DOMContentLoaded", () => {
  // your code here
  sub = document.getElementById("create-task-form")
  sub.addEventListener("submit", function(event){
    event.preventDefault()
  })
});
